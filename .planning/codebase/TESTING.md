# Testing

- Currently lacking an automated testing suite (Jest/Cypress/Playwright).
- Validation is performed through manual builds and linters (`npm run lint`, `npm run build`).
- Phase 4 targets E2E tests for the auth flow and chat integrations.
