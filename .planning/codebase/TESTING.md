# Testing

- Automated testing suite implemented using Vitest for unit tests (`npm run test:unit`) and Playwright for E2E tests (`npm run test:e2e`).
- Validation is performed through manual builds and linters (`npm run lint`, `npm run build`), along with the automated test suite.
- Playwright E2E tests cover the auth flow, quiz, and chat integrations.
